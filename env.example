# ========================================
# Gamified Learning Tracker - Environment Variables
# ========================================
# 
# Copy this file to .env and adjust values as needed
# cp env.example .env
#
# For local development, most defaults should work out of the box
# ========================================

# ========================================
# DATABASE CONFIGURATION
# ========================================

# PostgreSQL Database Settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=tracker

# Database Connection URL (auto-generated from above values)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/tracker?schema=public

# ========================================
# APPLICATION CONFIGURATION
# ========================================

# Environment Mode
NODE_ENV=development

# Application Port
PORT=3000

# Host Port for Docker (maps container port to host)
HOST_PORT=3000

# ========================================
# DEVELOPMENT SETTINGS
# ========================================

# Database Port Mapping (for local development)
# Maps host port 5433 to container port 5432
DB_HOST_PORT=5433

# Enable Debug Logging
DEBUG=false

# ========================================
# OPTIONAL CONFIGURATIONS
# ========================================

# Custom Database Name (optional)
# POSTGRES_DB=your_custom_db_name

# Custom Port to avoid conflicts (optional)
# HOST_PORT=3001
# DB_HOST_PORT=5434

# ========================================
# SECURITY NOTES
# ========================================
#
# ⚠️  IMPORTANT SECURITY REMINDERS:
#
# 1. NEVER commit .env files to version control
# 2. Use strong, unique passwords in production
# 3. Consider using Docker secrets for production deployments
# 4. The database is only accessible within the Docker network
# 5. External ports are only exposed in development mode
#
# ========================================
# QUICK START FOR LOCAL DEVELOPMENT
# ========================================
#
# 1. Copy this file: cp env.example .env
# 2. Start development: docker-compose -f docker-compose.dev.yml up
# 3. Access app at: http://localhost:3000
# 4. Database accessible at: localhost:5433
#
# ========================================

# Database Configuration
POSTGRES_PASSWORD=postgres
POSTGRES_DB=tracker

# Application Configuration
NODE_ENV=production
PORT=3000

# Port Mapping for Local Development
# Set HOST_PORT to avoid conflicts with other services
# For production, comment out HOST_PORT to disable external port exposure
HOST_PORT=3000

# Example for different port to avoid conflicts:
# HOST_PORT=3001

# Optional: Override default database name
# POSTGRES_DB=your_custom_db_name

# Security Notes:
# - Never commit .env files to version control
# - Use strong, unique passwords in production
# - Consider using Docker secrets for production deployments
# - The database is now only accessible within the Docker network
# - External port 5433 has been removed for security
